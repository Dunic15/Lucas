"""Compute-runtime domain: runpod_runtime (serverless GPU) and gpu_runtime
(photoreal GPU lifecycle). Moved from the flat app/ namespace; app.<name> still
resolves via a compatibility shim at the old path.
"""
