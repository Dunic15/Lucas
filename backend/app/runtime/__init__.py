"""Compute-runtime domain: runpod_runtime (serverless GPU), gpu_runtime
(photoreal GPU lifecycle), native_runtime (native action runtime). Moved from
the flat app/ namespace; app.<name> resolves via a compat shim at the old path.
"""
