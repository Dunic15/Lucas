"""Vendor/integration clients — thin wrappers around external services
(Recall, Google, Asana, Granola, Drive, Anam, Gemini ears, TTS, Gmail watcher,
vendor health, Jira, Graphiti). Moved here from the flat app/ namespace during
the backend restructure; ``app.<name>`` still resolves via a compatibility shim
at each old path.
"""
