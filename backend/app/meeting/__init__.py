"""Meeting-intelligence domain: meeting_state (readiness/step tracking),
decision (closing/browse detection), emotion, end_of_turn. Moved from the flat
app/ namespace; app.<name> resolves via a compat shim at the old path.
"""
