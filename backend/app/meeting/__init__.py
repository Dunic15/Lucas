"""Meeting-intelligence domain: the live-transcript understanding layer —
meeting_state (readiness/step tracking), decision (closing detection),
emotion, end_of_turn. Moved from the flat app/ namespace; app.<name> still
resolves via a compatibility shim at the old path.
"""
