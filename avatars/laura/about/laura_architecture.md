# Laura System Architecture

**Owner:** Product/engineering
**Applies to:** Current Laura backend and live meeting workflow
**Last reviewed:** 2026-07-16

## What Laura is

Laura is a callable AI process avatar. She joins a Zoom, Google Meet, or
Microsoft Teams meeting through Recall.ai, listens to the live transcript, and
answers when addressed. The answer is generated by the backend brain, grounded
in Laura's knowledge documents when they are relevant.

The product principle is separation of concerns: the backend is the brain,
Recall.ai is the meeting transport and transcript source, and the avatar layer
is only the face and mouth. This keeps the avatar layer swappable without
rewriting reasoning, retrieval, calendar entry, or post-meeting logic.

## Current production stack

The live backend runs on AWS App Runner in `eu-central-1`. The public backend
URL is `https://dhfgfe6yw6.eu-central-1.awsapprunner.com`.

The live brain is tiered by question type:

- **Fast live answers: Cerebras running `gemma-4-31b`** through its
  OpenAI-compatible API (`BRAIN_PROVIDER=cerebras`, a first-class provider).
  Cerebras was chosen for first-token latency (roughly 200-400 ms live) — inside
  a meeting, fast first-token response matters more than long-form writing quality.
- **Clearly complex questions** (analyze, compare, recommend) route to
  **Claude Haiku**.
- **Fresh-information questions** route to **Claude with native web search**.
- **Post-meeting artifacts** (summary, actions, follow-up email) use
  **Claude Sonnet** for quality; latency does not matter after the meeting.
- If Cerebras fails or rate-limits, a circuit breaker falls back to Claude
  Haiku so Laura never goes silent.

The face is an open-source 3D avatar (TalkingHead, WebGL, in-browser) rendered
by the `/talk` page, which Recall captures as the bot's camera. There is no
per-minute face vendor cost. The voice is **ElevenLabs** (a warm professional
female voice), synthesized server-side and shipped inside the speak messages
with word-level timings for lip-sync; fixed lines (acknowledgments, fillers)
are pre-synthesized at boot so they play instantly. If ElevenLabs is
unavailable the page falls back to free edge-tts.

Live transcription uses **Deepgram `nova-3` in multilingual mode** through
Recall (`RECALL_TRANSCRIPTION_PROVIDER=deepgram`), so meetings can mix Italian
and English. The Recall region is EU: `RECALL_API_BASE=https://eu-central-1.recall.ai`.

## Runtime flow

1. A meeting entry point starts a session: manual `/join`, calendar/Gmail
   watcher, or direct `POST /sessions/start`.
2. The backend asks Recall.ai to create a bot for the meeting.
3. Recall joins the meeting and renders Laura's avatar page as the bot camera.
4. Recall sends live transcript events (partials and finals) to the backend
   webhook. Partials drive instant acknowledgments and barge-in; finals drive
   answers.
5. The backend decides whether Laura should answer, retrieves relevant
   knowledge, and streams a grounded answer sentence by sentence. Each sentence
   is synthesized server-side and delivered with its audio.
6. While a meeting runs, Laura silently tracks process steps, decisions,
   owners, deadlines, and risks (MeetingState), keeps rolling notes of the
   whole conversation, and can flag one missing critical step as the meeting
   wraps up.
7. At session end the Recall bot leaves (per-minute billing stops) and the
   post-meeting artifact is built: summary, decisions, actions, missing steps,
   readiness score, and a draft follow-up email.

## Conversation behaviors

- **Interrupting her works:** if a human talks over her, she stops immediately
  and abandons the rest of that answer.
- **Follow-ups work:** being addressed by name always gets an answer, even
  seconds after her previous one.
- **Presence:** during long monologues she occasionally gives a small
  listening cue ("Mm-hm."); when addressed she acknowledges instantly while
  the answer generates.
- **Languages:** she replies in the language she was addressed in — Italian
  question, Italian answer.
- **Dismissal:** "Laura, you can leave" makes her say goodbye and leave the
  meeting, which also stops billing.
- **Calendar sight:** when the meeting owner's org has Google connected, she
  joins knowing their upcoming calendar (a bounded snapshot taken at session
  start) and can answer "when is my next meeting?" or "do I have a meeting
  with Marco?" via her `upcoming_meetings` tool — no live network call.
- **Echo immunity:** she never answers her own voice coming back through a
  participant's open mic or a second transcription source — near-verbatim
  echoes and re-worded re-transcriptions are both recognized and dropped.
- **Personal workspaces:** every user who signs in gets their OWN workspace
  (their meetings, artifacts, connections, free minutes) — colleagues on the
  same company domain are NOT grouped together. Meetings she is invited to
  are attributed to the inviting person's workspace: their meter runs, their
  tools act. Team/shared workspaces are a planned later feature.

## Owned boundaries

Answer quality is owned by:

- `avatars/laura/knowledge/*` (real process docs; `about/*` covers Laura herself)
- per-org private indexes (an org's own ingested docs — Drive/uploads — live in
  a separate per-tenant index merged at retrieval; orgs never see each other's)
- `backend/app/rag.py`
- `backend/scripts/ingest.py`
- the grounding and prompt logic in `backend/app/brain.py`

Avatar rendering, voice, SSE/polling delivery, and Recall bot configuration
are separate concerns. If Laura generates a good answer but the meeting cannot
hear it, that is not a brain/RAG problem.

## Important distinction: brain problem versus delivery problem

If logs show answer latency lines or generated text but Laura does not speak,
the brain worked and the issue is downstream delivery to the avatar mouth.

If Laura speaks but gives a weak, irrelevant, or generic answer, inspect the
knowledge docs, retrieval scores, and prompt. The most common answer-quality
failure is not the LLM provider; it is missing or poorly retrieved context.

## Common questions

**"What model are you built on?"**
The live brain is Cerebras running gemma-4-31b for speed, with Claude models
for complex reasoning, web search, and the post-meeting artifact. The stack is
deliberately pluggable — providers can be swapped by configuration.

**"Is it still in AWS?"**
Yes — AWS App Runner in `eu-central-1`.

**"Do we need ElevenLabs?"**
Yes for the premium voice: ElevenLabs synthesizes Laura's voice server-side
with word timings for lip-sync. If it is unavailable, a free fallback voice
keeps her speaking.

**"What happened to Groq and Anam?"**
Groq was the previous fast-brain provider; it was replaced by Cerebras (same
OpenAI-compatible client, faster and more consistent). Anam was the previous
paid face vendor; it was replaced by the free open-source TalkingHead page and
is kept only as a fallback.
