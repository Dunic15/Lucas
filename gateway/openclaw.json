{
  "_comment": [
    "OpenClaw gateway configuration — DEVELOPMENT ONLY.",
    "The primary model is stated EXPLICITLY here rather than relying on any",
    "upstream default, so a new image version cannot silently change which",
    "model executes Laura's approved actions.",
    "No secrets in this file: API keys arrive from SSM via secrets.env."
  ],

  "agents": {
    "defaults": {
      "model": "anthropic/claude-opus-5"
    },
    "laura-executor-test": {
      "model": "anthropic/claude-opus-5",
      "description": "Executes post-meeting actions Laura has already approved and claimed.",
      "maxToolRoundtrips": 8
    }
  },

  "models": {
    "anthropic/claude-opus-5": {
      "provider": "anthropic",
      "id": "claude-opus-5"
    }
  },

  "server": {
    "auth": {
      "_comment": [
        "Authentication is MANDATORY. required=true means an unauthenticated",
        "request is rejected before reaching any agent — the gateway must",
        "never be an open relay to a paid model, even briefly, even inside a",
        "VPC. The token itself comes from SSM (OPENCLAW_GATEWAY_TOKEN)."
      ],
      "required": true,
      "scheme": "bearer",
      "tokenEnv": "OPENCLAW_GATEWAY_TOKEN"
    },
    "cors": {
      "_comment": "No browser origin needs this API; only laura-backend-next calls it.",
      "enabled": false
    },
    "requestTimeoutSeconds": 90,
    "maxConcurrentSessions": 8
  },

  "state": {
    "_comment": "Persisted to the EBS-backed volume; survives restarts and image bumps.",
    "dir": "/state",
    "persistSessions": true
  },

  "logging": {
    "_comment": [
      "Never log message content: Laura's plans carry meeting-derived",
      "material. Structured metadata only."
    ],
    "level": "info",
    "format": "json",
    "logRequestBodies": false,
    "logResponseBodies": false
  }
}
