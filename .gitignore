# Secrets
.env
*.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Local data / generated artifacts
# Per-avatar RAG indexes are generated by backend/scripts/ingest.py
avatars/*/.index*.json
avatars/*/.about-index.json
backend/data/
data/
*.log
.playwright-mcp/

# Node (avatar page tooling, if added later)
node_modules/
.next/

# Local Lovable editor checkout copied into lovable/laura-meeting-expert for git
lovable/lucas-live-expert/
lovable/laura-meeting-expert/package-lock.json

# OS / editor
.DS_Store
.idea/
.vscode/

# Local dev/preview scratch dumped at repo root (site design work).
# Root-scoped only (leading slash) so real assets under docs/assets/ etc. stay tracked:
#   - Framer/site preview screenshots (hero-*, preview-*, final-*, talk-*, top-*, nav-*, product-*, etc.)
#   - Framer MCP exports (framer-*.yml) and publish-panel exports
# These are throwaway captures from iterating on the landing site; commit real assets to docs/assets/.
/*.jpeg
/*.jpg
/*.png
/framer-*.yml
/publish-panel.yml

# Local A/V test clips + scratch capture dirs (media, not source; commit real assets to docs/assets/)
avatar-test-clips/
desktop-laura-stuff/
voice-previews/
images laura/

# GTM lead lists; real prospect emails (PII); keep strategy .md docs, never the raw contact data
docs/gtm/*.xlsx
docs/gtm/recipients.csv
