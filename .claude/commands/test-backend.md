---
description: Run the backend test suite + offline pipeline checks and summarize failures
---

Use the **backend-tester** agent to run the backend test suite and the offline
pipeline checks (ingest → ask → simulate). Summarize results: pass/fail counts,
then each failure with the exact assertion/output; no fixes, just the report.
